##########################################################
requires:
  - PKG_BAD

##########################################################
path:
  DATA_LOCAL:               ./data

shell:

  PATH:                     [prepend, .]
  DEPENDENCY:               ${path:DATA_LOCAL}


##########################################################
config:
  CSV_FILE_PATH:            [prepend, .]
  OTQ_FILE_PATH:            [prepend, .]
  TEST:                     ENV ${TEST}




##########################################################
